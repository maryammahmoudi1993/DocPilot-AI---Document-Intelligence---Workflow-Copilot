import { Sparkles } from 'lucide-react';
import { RoutePlaceholder } from '@/components/layout/RoutePlaceholder';

export function AiAssistantPage() {
  return <RoutePlaceholder title="AI Assistant" description="Ask grounded questions with citations from indexed documents." icon={Sparkles} />;
}
